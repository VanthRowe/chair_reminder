# AGENTS.md

## 项目

chair_reminder：座椅久坐提醒器，GD32L233RC（Cortex-M23）裸机固件。Keil MDK 工程（`chair_reminder.uvprojx`，ARMClang V6），C 语言。芯片资料见 `doc/`（数据手册 + 用户手册中文版）。

## 硬性约定

0. **禁止自动提交 git**：只有用户明确说"提交 git"（或同义表达）时才执行 `git add` + `git commit`；功能完成、编译通过等任何场景都不自动提交。提交后汇报改动范围与提交信息。
1. 分层纪律（驱动与业务隔离，为换芯片/换板留活口）：
   - `drivers/bsp/`：外设裸驱动封装，只允许这里直接操作寄存器/固件库；
   - `drivers/device/`：具体设备逻辑（如 LED），只调 bsp；
   - `components/`：通用组件（如 log），与具体业务无关；
   - `app/`：应用业务，只调 device/components，禁止直接调固件库；
   - `RTE/`：固件库与启动文件，**禁止修改**。
2. 新增 `.c` 文件必须同步登记进 `chair_reminder.uvprojx`（对应分组 + FilePath），新目录同步加 IncludePath，否则 Keil 编译不到。
3. 硬件资源（引脚、定时器、ADC/DAC 通道、DMA 等）必须先登记到本文件"资源分配表"再写代码，防止两处占用同一资源。
4. 自测分级（防小改动背着全流程跑）：
   - **代码改动**：必须 Keil CLI 编译验证 `0 Error(s)`（命令见下）；涉及硬件行为的改动（引脚、时序、通信）需用户在板上验证现象，AI 不得宣称"已验证硬件效果"；
   - 纯文档/注释改动可跳过编译。
5. 范围纪律：只改动与本任务相关的代码与文档；发现题外问题先向用户报告，不随手重构或"优化"无关代码。
6. 芯片行为/需求在数据手册或用户描述中未明确时，列为待确认项向用户求证，禁止自行假设后写入代码（合理 ≠ 正确，时序和电气参数必须查手册）。
7. AI 反复犯的写法错误，纠正后把规则补进本文件禁区清单（见下），不只修当次。
8. 裸机无单元测试框架：自测 = 编译通过 + log 组件串口输出佐证运行行为；bug 修复先复现（日志或最小代码路径）再修。
9. 规则治理：本文件只增不删会失效——定期修剪：AI 已不再犯的删除、同类合并、能机制化的转交脚本/门禁。
10. 文档只写当前方案，不写修改时间点与变更历史（历史由 git 承担），禁止"原为 X、本次改为 Y"式对比表述。

## 禁区清单

（暂无，按约定 7 累积）

## 资源分配表

| 资源 | 引脚/通道 | 用途 |
|---|---|---|
| USART0 | PA9(TX)/PA10(RX), AF7, 115200 | log 日志输出 |
| TIMER1 CH1/CH2/CH3 | PA1/PA2/PA3, 1kHz PWM | RGB LED 呼吸灯 |
| TIMER2 | — | log 时间戳（1ms 自由运行） |
| TIMER6 | — | 轮询延时（bsp_timer） |

## 汇报格式

每次执行完任务后，按以下格式汇报：

```
老板，已执行完成。

<做了什么：变更内容、文件范围等>

<其他补充事项：需要用户决定的问题、下一步建议等（如无则省略）>
```

## 常用命令

- 编译：`"/d/Keil_v5/UV4/UV4.exe" -b chair_reminder.uvprojx -j0 -o build_log.txt`（看输出末尾 `0 Error(s)` 为通过；退出码 1 仅表示有警告，需看日志确认非新增），编译后删除 `build_log.txt`
- 烧录/调试：Keil 界面内用 J-Link，AI 不执行烧录
- 日志查看：串口工具接 USART0（PA9/PA10），115200 8N1

## 代码规范

- C99，命名沿用现有风格：文件与函数加模块前缀（`bsp_xxx`、`device_xxx`、`log_xxx`），类型用 `<stdint.h>` 定宽类型；
- 头文件用 `#ifndef` 保护；对外接口在 `.h`，内部函数加 `static`；
- 常量用 `#define` 集中放在文件头部并加注释，禁止魔法数散落代码中；
- 默认不写注释，只在硬件时序、非直觉寄存器配置处写必要注释（注释用中文）。
